version: '3.8'

services:
  bot:
    build: .
    container_name: source_search_bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./database.db:/app/database.db
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"